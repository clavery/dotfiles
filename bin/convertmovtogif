#!/usr/bin/env bash

name=$(basename "$1" .mov)
/usr/local/bin/ffmpeg -i "$1" -pix_fmt rgb24 -r 10 -f gif - | /usr/local/bin/gifsicle --optimize=3 --delay=7 > $TMPDIR/out.gif
mv $TMPDIR/out.gif ~/Documents/screencasts/${name}.gif
